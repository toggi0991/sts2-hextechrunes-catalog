# TODO

- [ ] **직업 전용 카드/룬 검색**: 특정 직업(아이언클래드/사일런트/리젠트/디펙트/네크로바인더) 전용 항목만 필터링. `relic_collection.json`에 `HEXTECH_CHARACTER.IRONCLAD` 같은 서브카테고리 라벨은 있지만, 항목별로 어느 직업 전용인지 매핑하는 데이터는 아직 추출하지 못함 — DLL(C#) 안에 로직이 있을 가능성이 높아 별도 디컴파일 필요.
- [ ] **증강 등급(실버/골드/프리즘) 검색**: 모루(forge) 계열은 `SILVER_`/`GOLD_`/`PRISMATIC_` 접두사로 등급이 ID에 있어 바로 필터 가능. 하지만 일반 룬은 등급이 항목별로 고정되어 있지 않고 게임 내에서 동적으로 스케일링되는 구조라(`build_data.py` 작업 중 확인), 룬까지 등급 필터를 걸려면 별도 등급 배정 로직을 DLL에서 확인해야 함.
